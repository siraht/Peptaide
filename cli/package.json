{
  "name": "peptaide-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "peptaide": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:usage": "npm run build && node dist/index.js --help"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "typescript": "^5.9.2",
    "vitest": "^4.0.18"
  }
}
